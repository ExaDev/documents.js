// Alignment and LayoutFont now both live in document-content-model. Alignment: this file's own logic was ported to ooxml.js first (since ContentParagraph needs the same type ooxml.js's readDocx/readPptx produce directly), then on to document-content-model -- re-exported here under the same name so every existing caller keeps resolving it unchanged. LayoutFont: a requested font, independent of any concrete rendering backend -- src/pdf/fonts.ts resolves this to one of the 14 standard PDF faces at write time; src/pdf/font-read.ts produces one from a PDF's own /BaseFont + /FontDescriptor at read time. It moved to document-content-model because LayoutText (part of the pivot model) needs the same font field, but the standard-14 resolution logic that consumes it stays here, PDF-specific and out of document-content-model's scope.
export type { Alignment, LayoutFont } from 'document-content-model';
export { AlignmentSchema, DEFAULT_LAYOUT_FONT, LayoutFontSchema } from 'document-content-model';
