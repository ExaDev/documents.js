node_modules/
.turbo/
coverage/
.eslintcache
*.tsbuildinfo
.DS_Store
.env
.env.local
.playwright-mcp/
playwright-report/
test-results/

# dist/ is deliberately absent: every package ignores its own build output in its own .gitignore, and an entry here would silently cover a package that hasn't stated the decision itself.
