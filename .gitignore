node_modules/
.turbo/
coverage/
.eslintcache
*.tsbuildinfo
.DS_Store
.env
.env.local
playwright-report/
test-results/

# dist/ is deliberately absent: markdown-codec tracks its own dist/ on purpose (see that package's .gitignore), and every package that does ignore its build output already says so in its own .gitignore. Ignoring dist/ from here would make the workspace root disagree with both.
