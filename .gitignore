node_modules/
.turbo/
coverage/
.eslintcache
*.tsbuildinfo
.DS_Store
.env
.env.local
playwright-report/
test-results/

# dist/ is deliberately absent: every package that ignores its build output already says so in
# its own .gitignore, so the exclusion lives beside the artefact it ignores.
