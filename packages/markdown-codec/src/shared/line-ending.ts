// CommonMark's own line-ending grammar (spec 0.31.2, "Preliminaries" -> "Lines"): "a line ending is a line feed (U+000A), a carriage return (U+000D) not followed by a line feed, or a carriage return and a following line feed". A plain `.split("\n")` recognises only the first of those three -- a lone CR-terminated line (the classic Mac OS line ending some legacy document producers still emit) slips past it entirely, reappearing as a run of ordinary text rather than a line boundary. Shared by every module in this package that needs to split raw text into CommonMark lines: src/block/block.ts (splitting a document's own source into candidate block lines) and src/lower/front-matter.ts (splitting a recognised front-matter block's own body) on the read side, and src/emit/emit.ts (splitting a rendered heading's own text back into lines to check for an embedded blank one) on the write side -- one pattern, so a fix to the rule can never happen in only one of the three call sites.
export const LINE_ENDING_PATTERN = /\r\n|\n|\r/;

// The escaped hard-break spelling (escapeMarkdownText's own backslash immediately before the line ending it terminates) widened to every line ending LINE_ENDING_PATTERN itself recognises, not merely LF -- composed from LINE_ENDING_PATTERN.source so the two can never drift apart. This package's own escapeMarkdownText always spells a hard break with a trailing LF, but a markdown-format residue run (a foreign producer's own preserved source text, re-emitted verbatim by src/emit/inline.ts's renderLeaf) can carry the identical backslash-escape spelling against a CRLF or lone-CR line ending just as legitimately. Stripping the backslash with an LF-only pattern ahead of a LINE_ENDING_PATTERN-based collapse leaves that backslash behind as a stray literal character once the wider split removes the CRLF/CR line ending out from under it, rather than removing escape and line ending together as one unit the way the LF case does.
export const ESCAPED_HARD_BREAK_PATTERN = new RegExp(
  `\\\\(?:${LINE_ENDING_PATTERN.source})`,
  "g",
);
