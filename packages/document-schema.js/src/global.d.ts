// Replaced with a literal string at build time by tsdown.config.ts's own `define` (and mirrored by vitest.config.ts's `define` for `pnpm test`, which runs directly against src/ rather than through tsdown) -- see src/schema-io.ts's schemaUriFor().
declare const __PACKAGE_VERSION__: string;
