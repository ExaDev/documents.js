{
  // A no-op Worker whose only purpose is to host ppt-codec's source as the module graph the workers test suite (test/workers/**) imports against under the real workerd runtime -- there is no fetch handler to exercise, only the library's own [MS-PPT] record-tree read running inside a Cloudflare Workers isolate to prove it carries zero Node-only API usage.
  "name": "ppt-codec-workers-test",
  "main": "src/index.ts",
  "compatibility_date": "2024-09-23",
}
