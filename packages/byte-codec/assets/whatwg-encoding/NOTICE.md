# WHATWG Encoding Standard index tables

`indexes.json` fetched directly from `https://encoding.spec.whatwg.org/indexes.json`, fetched 2026-09-23. Server-reported `Last-Modified: Thu, 21 May 2026 05:11:18 GMT`, `ETag: "6a0e93f6-81725"` (captured from the response headers at fetch time, recorded here since the WHATWG Encoding Standard is a Living Standard with no discrete version numbers or tags — the file has no fixed "version" to cite beyond the point-in-time snapshot this fetch captured, the same reason the sibling `html-entities/NOTICE.md` gives for its own WHATWG source). These are the pointer-to-code-point index tables the Encoding Standard's own decoder algorithms (`https://encoding.spec.whatwg.org/#indexes`) reference by name — `index jis0208`, `index jis0212`, `index Big5`, `index EUC-KR`, `index gb18030`, `index gb18030 ranges` — used here to regenerate `src/text/dbcs-tables.ts` for the legacy multi-byte and double-byte CJK decoders (Shift_JIS, EUC-JP, EUC-KR, Big5, GBK, gb18030, ISO-2022-JP; ExaDev/documents.js#1388). Vendored in full and unmodified, though only the `big5`, `euc-kr`, `gb18030`, `gb18030-ranges`, `jis0208` and `jis0212` keys are actually read by the generator — the file also carries every legacy single-byte index (`iso-8859-*`, `koi8-r`, `windows-125x`, and so on), which this package's own `legacy-single-byte-tables.ts` (ExaDev/documents.js#1361) sourced from ICU via a platform `TextDecoder` instead, not from this file.

## Licence

Per the WHATWG Encoding Standard's own footer (`https://encoding.spec.whatwg.org/`, read in full before vendoring):

> Copyright © WHATWG (Apple, Google, Mozilla, Microsoft). This work is licensed under a Creative Commons Attribution 4.0 International License. To the extent portions of it are incorporated into source code, such portions in the source code are licensed under the BSD 3-Clause License instead.

`indexes.json` is data incorporated directly into this package's own generated source (`src/text/dbcs-tables.ts`, produced by `scripts/generate-dbcs-tables.mjs`), so the WHATWG's own footer clause applies: the BSD 3-Clause License, not CC-BY 4.0, governs this vendored copy, matching `html-entities/NOTICE.md`'s identical reasoning for the sibling WHATWG asset in `markdown-codec`. This vendoring is unmodified verbatim redistribution of the table for use as this package's own decoder-generation data and test fixtures, with attribution recorded here.
