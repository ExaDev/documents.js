// Alignment and LayoutFont now both live in document-content-model. Alignment: this file's own logic was ported to ooxml.js first (since ContentParagraph needs the same type ooxml.js's readDocx/readPptx produce directly), then on to document-content-model -- re-exported here under the same name so every existing caller keeps resolving it unchanged. LayoutFont: a requested font, independent of any concrete rendering backend -- resolved to one of the 14 standard PDF faces at write time, and produced from a PDF's own /BaseFont + /FontDescriptor at read time, by the external pdf-codec dependency's own fonts.ts/font-read.ts (formerly src/pdf/fonts.ts/font-read.ts), which imports LayoutFont straight from document-content-model rather than through this re-export. LayoutFont itself moved to document-content-model because LayoutText (part of the pivot model) needs the same font field, but the standard-14 resolution logic that consumes it is PDF-specific and stays out of document-content-model's scope.
export type { Alignment, LayoutFont } from 'document-schema.js';
export { AlignmentSchema, DEFAULT_LAYOUT_FONT, LayoutFontSchema } from 'document-schema.js';
