// Colour primitives now live in document-content-model (this file's own logic, including the DrawingML shade/tint/lumMod/lumOff transform maths, was ported to ooxml.js first, then on to document-content-model, since document-content-model's own content vocabulary resolves colour cascades directly now) -- re-exported/aliased here under documents.js's own established `LayoutColor` name so every existing caller (src/edit/*, src/pdf/*, src/index.ts, src/layout/shared.ts) keeps resolving them unchanged. `ColorTransform`/`applyColorTransforms` are not re-exported: nothing in documents.js calls them once src/ooxml/drawingml.ts (their only caller) is gone -- that resolution logic lives entirely in ooxml.js's own readDocx/readPptx now.
export type { Color as LayoutColor } from 'document-content-model';
export { colorToRgbHex, COLOR_BLACK, ColorSchema as LayoutColorSchema, rgbHexToColor } from 'document-content-model';
